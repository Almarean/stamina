{% extends 'navbar.html.twig' %}

{% block body %}
    {{ parent() }}
    <div class="container-fluid">
        <section class="page">
            {% if type_items == 'bestiary' %}
                <h1 class="h3 h1-responsive text-center my-5 brown-text">Bestiaire</h1>
            {% else %}
                <h1 class="h3 h1-responsive text-center my-5 brown-text">Zones</h1>
            {% endif %}
            <article class="my-5">
                {% for item in items %}
                    <div class="row col-md-8 m-auto">
                    {% if item.id % 2 == 0 %}
                        <div class="col-lg-5">
                            <div class="view overlay rounded z-depth-2 mb-lg-0 mb-4">
                                <img class="img-fluid" src="{{ asset('images/items/' ~ item.image) }}" alt="Image non chargée.">
                                <a><div class="mask rgba-white-slight"></div></a>
                            </div>
                        </div>
                        <div class="col-lg-7">
                            <h3 class="font-weight-bold mb-3 brown-text"><strong>{{ item.name }}</strong></h3>
                            <p>{{ item.description }}</p>
                        </div>
                    {% else %}
                        <div class="col-lg-7">
                            <h3 class="font-weight-bold mb-3 brown-text"><strong>{{ item.name }}</strong></h3>
                            <p>{{ item.description }}</p>
                        </div>
                        <div class="col-lg-5">
                            <div class="view overlay rounded z-depth-2">
                                <img class="img-fluid" src="{{ asset('images/items/' ~ item.image) }}" alt="Image non chargée.">
                                <a><div class="mask rgba-white-slight"></div></a>
                            </div>
                        </div>
                    {% endif %}
                    </div>
                    <hr class="my-5 col-md-8">
                {% endfor %}
            </article>
        </section>
    </div>
    {{ include('footer.html.twig') }}
{% endblock %}